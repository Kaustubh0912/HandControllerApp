<resources>
    <!-- Base application theme -->
    <style name="Theme.HandController" parent="Theme.MaterialComponents.DayNight">
        <!-- Your existing theme attributes -->
    </style>

    <!-- Theme with no action bar -->
    <style name="Theme.HandController.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- Optional: System bars color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>